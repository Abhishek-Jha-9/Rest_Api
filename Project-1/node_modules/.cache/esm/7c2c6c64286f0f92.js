let JWT_SECRET,jwt;_286‍.x([["default",()=>_286‍.o]]);_286‍.w("../config",[["JWT_SECRET",["JWT_SECRET"],function(v){JWT_SECRET=v}]]);_286‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

class JwtService {
  static sign(payload, expiry = "60s", secret = JWT_SECRET) {
    return jwt.sign(payload, secret, { expiresIn: expiry });
  }
  static validate(token, secret = JWT_SECRET) {
    return jwt.verify(token, secret);
  }
}
_286‍.d(JwtService);
