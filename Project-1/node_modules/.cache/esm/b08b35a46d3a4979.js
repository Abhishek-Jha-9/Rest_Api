let Joi,errorHandler,User,RefreshToken,CustomErrorHandler,bcrypt,JwtService,REFRESH_SECRET;_6d3‍.x([["default",()=>_6d3‍.o]]);_6d3‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_6d3‍.w("../../middlewares/errorHandler",[["default",["errorHandler"],function(v){errorHandler=v}]]);_6d3‍.w("../../models",[["User",["User"],function(v){User=v}],["RefreshToken",["RefreshToken"],function(v){RefreshToken=v}]]);_6d3‍.w("../../services/CustomeErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_6d3‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_6d3‍.w("../../services/JWTservice",[["default",["JwtService"],function(v){JwtService=v}]]);_6d3‍.w("../../config",[["REFRESH_SECRET",["REFRESH_SECRET"],function(v){REFRESH_SECRET=v}]]);






// import RefreshToken from "../../models";

const loginControllers = {
  async login(req, res, next) {
    // validate

    const loginSchema = Joi.object({
      email: Joi.string().required(),
      password: Joi.string()
        .pattern(new RegExp("^[a-zA-Z0-9]{3,30}$"))
        .required(),
    });

    // const { email, password } = req.body;

    const { error } = loginSchema.validate(req.body);

    if (error) {
      return next(error);
    }
    try {
      const user = await User.findOne({ email: req.body.email });
      if (!user) {
        return next(CustomErrorHandler.wrongCredentials());
      }
      // compare the password
      const matchPassword = await bcrypt.compare(
        req.body.password,
        user.password
      );

      if (!matchPassword) {
        return next(CustomErrorHandler.wrongCredentials());
      }
      // geneterate token if username and password matched
      const accessToken = JwtService.sign({
        _id: user._id,
        role: user.role,
      });
      const refresh_Token = JwtService.sign(
        { _id: user._id, role: user.role },
        "1y",
        REFRESH_SECRET
      );
      //  database whitelist
      await RefreshToken.create({ token: refresh_Token });
      res.json({ accessToken, refresh_Token });
    } catch (error) {
      return next(error);
    }
    // return "hello";
  },
  async logout(req, res, next) {
    try {
      // validate before delete
      const refreshSchema = Joi.object({
        refresh_Token: Joi.string().required(),
      });

      const { error } = refreshSchema.validate(req.body);

      if (error) {
        return next(error);
      }
      await RefreshToken.deleteOne({ token: req.body.refresh_Token });
    } catch (error) {
      return next(new Error("Something went wrong in the  database"));
    }
    res.json({ message: "logout Successful!" });
  },
};
_6d3‍.d(loginControllers);
